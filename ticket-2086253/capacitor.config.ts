import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ticket-2086253',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
